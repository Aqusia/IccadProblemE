# Current Plan

## 當前目標

- `doing` 建立題目、paper、web research 的可檢索知識底稿
- `todo` 確認 testcase input/output 細節
- `todo` 設計 baseline solver 架構
- `todo` 開始實作 parser + data model + evaluator

## 下一步

- 從 `E_20260414.pdf` 補齊 input/output 格式
- 在 `Route A` 與 `Route B` 間決定 baseline
- 決定 solver 表示法與 search 流程
- 若專案 code repo 確認後，建立實作區映射

## 完成條件

- 有一份可直接依照題目規格實作的技術規格
- 有一版可跑 testcase 的 baseline solver
